<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsaap</title>
    <link rel="stylesheet" href="/5_Node - 30/DAY - 40/NODE WITH MONGODB/public/style.css">
</head>
<body>
    <h3>All Chats</h3>
    <form method = "get" action = '/chats/new'>
        <button>New Chat</button>
    </form>
    <% for(let chat of chats){ %>
        <div class = "chat">
            <p> From : <i><%= chat.from %></i></p>
            <div class = "msg">
                <form method = "get" action="/chats/<%= chat.id %>/edit">
                    <button>Edit</button>
                </form>
                <form method = "post" action="/chats/<%= chat.id %>/?_method=DELETE">
                    <button>Delete</button>
                </form>
                <p><%= chat.msg %></p>
            </div>
            <p>Received by <i><%=chat.to %></i></p>
        </div>
        <p><%= chat.created_at.toString().split(" ")[4] %>      <!-- split => it find space, after 4's space it will give value -->
        <p><%= chat.created_at.toString().split(" ").slice(0,4).join("-") %><hr>    <!-- it find space, after 4's space it will give 0-4 all value then join all 4 values between(-) -->
    <% } %>
    <br><br>
</body>
</html>